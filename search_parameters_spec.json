{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SEEK Scraper Search Parameters",
  "description": "Configuration for a job search query on SEEK.",
  "type": "object",
  "properties": {
    "keyword": {
      "description": "The job title, skill, or keyword to search for.",
      "type": "string",
      "examples": ["Data Analyst", "Python Developer"]
    },
    "location": {
      "description": "The city or region to search within.",
      "type": "string",
      "examples": ["Sydney", "Melbourne All"]
    },
    "max_pages": {
      "description": "The maximum number of result pages to scrape.",
      "type": "integer",
      "default": 10,
      "minimum": 1
    },
    "work_type": {
      "description": "The type of employment.",
      "type": "string",
      "enum": ["Full Time", "Part Time", "Contract/Temp", "Casual/Vacation"],
      "examples": ["Full Time"]
    },
    "salary_range": {
      "description": "The desired salary range, formatted as a string.",
      "type": "string",
      "pattern": "^\\d+k-\\d+k$",
      "examples": ["100k-150k"]
    }
  },
  "required": ["keyword"]
}
